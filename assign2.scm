(define (author)
    (println "AUTHOR: Thomas Willingham twillingham@crimson.ua.edu")
    )

(define (iterator # $x items $)
    (define (f) 
        (lambda (x)
            (while (valid? $)
                   (car $)
                   )
            )
        )
    )






(define (run1)
  )

(define (run2)
  )

(define (run3)
  )

(define (run4)
  )

(define (run5)
  )

(define (run6)
  )

(define (run7)
  )

(define (run8)
  )

(define (run9)
  )

(define (run10)
  )
